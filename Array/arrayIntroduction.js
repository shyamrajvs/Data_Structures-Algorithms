//Here’s a comprehensive list of array methods in JavaScript, along with brief descriptions of what they do:

//Mutating Methods

/*1.push(element1, element2, ...)(https://github.com/shyamrajvs/Data_Structures-Algorithms/blob/main/Array/Mutating%20Methods/push.js)

    Adds one or more elements to the end of the array and returns the new length.

2.pop()

    Removes the last element from the array and returns it.

3.shift()

    Removes the first element from the array and returns it.

4.unshift(element1, element2, ...)

    Adds one or more elements to the beginning of the array and returns the new length.

5.splice(start, deleteCount, item1, item2, ...)

    Changes the contents of an array by removing or replacing existing elements and/or adding new elements.

6.fill(value, start, end)

    Fills all the elements of an array from a start index to an end index with a static value.

7.reverse()

    Reverses the elements of an array in place.

8.sort(compareFunction)

    Sorts the elements of an array in place and returns the array.*/